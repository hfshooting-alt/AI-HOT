# 增量回放与来源队列修正

## 增量回放（无新增API调用）

依据已发布批次及其私有company-evidence.json，还原199篇公司抽取输入，复用199条对应版本的成功缓存。按北京时间2026-09-14零点切分：前段114篇、后段85篇。结果：分段增量与全量合并一致；再次合并不产生变化；前段已有公司全部保留。测试不改写文章日期，不冒充未来批次的真实运行。

## 止损与排队

此前SOURCE_CONCURRENCY=3，CostCircuit累计3次止损后拒绝其余任务创建；该批实际启动5个，4个止损，ZFinance成功，剩15个未启动。用户选择最多并发3个、全部依次启动。因此移除普通费用止损的跨来源取消，保留单源止损与停止失败等系统性保护。新增20个模拟来源全部止损的离线回归：20个均启动，峰值并发3。旧未启动占位不锁死首次启动；实际失败尝试仍需显式付费重试。

已核实文章在轮询时逐篇保存；停止成功后再只读回收一次（最多10页），保留合法结果。没有返回结构化标题、URL、时间等的浏览叙述无法作为文章；通过发现不等于正文已获取或最终入库。

## 17项待归属抽查

- Grok与Grok Bot在待归属区出现，同时公司清单已有相关产品；Grok还同时出现在xAI与特斯拉名下，不能单凭产品名命中自动推定所有权。
- Fable/Fable 5.1、Seedance/Seedance 2.5涉及系列与版本，尚未强行合并。
- Recurrent Looped Transformer采集输入明确描述论文架构方法，需明确是否应进产品库。
- commit-rewriter、PaperRoute等独立项目保留来源，开发者姓名不当公司。
- 本轮不凭模型常识补公司归属、不改线上分类。产品归属与集成关系、论文方法的收录边界已提交用户确认。
