# 2026-09-20 费用保护、成功结果恢复与两篇补录

## 本轮范围与费用

复用智东西先前单源 canary 的两篇已核实文章，未创建任何新的 Manus 任务。两篇分别完成相关性、摘要分类、公司抽取，共 6 次 DeepSeek-V4-Pro 请求，服务端返回 28,434 tokens；没有自动重试。两篇正文分别为 6,604 和 4,680 字符，第二篇追加一次免费 HTTP 读取。

新闻窗口仍为北京时间 2026-09-19 09:30 至 2026-09-20 09:30；原文时间分别为 2026-09-19 16:30:02、12:30:03。原单源 Manus 成本 21 credits 已在先前记录中披露，不重复计入本轮新增费用。

## 发布候选

- 原 238 篇新闻标题、摘要、链接、原始时间全部保留，追加两篇后 240 篇：232 AIHOT、8 Manus。
- 智东西标为 partial，保留止损及补录来源；不把旧定时运行失败改写成成功，不以两个样本宣称完整 24 小时覆盖。
- 两篇归大厂动态；第二篇摘要区分架构介绍和首次发布，保留昇腾960风冷、液冷超节点在2027年第2、3季度上市的限定，性能数字归因华为。
- 公司库 576 公司或组织、1,221 个具名关联产品，另 164 待归属、2 排除。OpenCode 旧行没有公司或法人证据，新文明确为开源框架，因此移入待归属，原两篇新闻与集成关系保留。
- 华为移除新证据中6项协议、数值格式或方法名称，保留具名硬件与开发工具。修复 Atlas 650E 原空引文的归属关系，依据相邻两句原文。
- 科大讯飞新文章没有国家证据，不写入模型自行补出的“中国”；业务与新闻关联保留。华为 business 抽取误把 openEuler 的部署量写到鲲鹏，已通过绑定文章与原文的规则在合并前纠正，原模型缓存保留。
- 融资输入集合仍为38篇，与既有33行表一致，不再次调用模型。所有既有历史归档文章ID保留；原公司资料补全报告、历史恢复溯源保留。

私人请求台账、原始正文、原模型输出、审阅及候选位于 work/zhidx-chain-20260920，不随网站公开。人工规则在 config/quality_review.json，绑定文章、摘要指纹或原文证据，不改写模型缓存版本。

## 代码修复与验证

公司背景补全独立账本在页面读取和模型请求前预留费用；成功建议与原日期先保存，再等待整个候选发布。失败、超时、进程中断或另起运行不退回额度。GitHub 通过保存并读回每日租约以及保留的工作流历史防止缓存消失后再次开放当天批次；范围和限制见[操作手册](../operations/COMPANY_WEB_RESEARCH.md)。

加密恢复保存 ledger/results/model-cache 和窄范围请求占位，恢复只回放成功建议，不搜索或请求模型，不把恢复日期写成资料日期。证据列表仅调序不构成公司资料更新，Google、微软、高通本轮原日期均保留。

本地最终 533 项 Python 离线回归和43项 Node 测试通过。隔离加密演练使用真实候选校验，验证未发布建议回收、原日期、缺缓存拒绝、密文篡改拒绝、正式数据不变；付费请求0、页面请求0。现有云端演练工作流会因脚本修改在提交后执行两个独立 runner 的同一验证，实际结果见该提交 Actions。

## 尚未验收

GitHub 北京时间09:30准点触发仍未解决；不新增外部定时器。来源完整覆盖仍未通过，本次只证明两篇真实文章能经过模型加工、复核和发布候选门禁。下次真实定时运行需核对创建时间、固定窗口、AIHOT与Manus来源、公司增量、费用以及Pages实际部署。
