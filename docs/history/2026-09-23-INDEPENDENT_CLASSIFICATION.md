# 2026-09-23 全部文章独立打标修复

旧流程在相关性筛选后才进行摘要和分类，导致15篇非精选正文一直显示未分类；另10篇缺正文、1篇Jev未通过发布证据校验。用户确认将打标与精选解耦，并删除热点榜、AI日报栏目。

## 本轮结果

- 固定沿用9月22日批次窗口：北京时间2026-09-21 18:02:50至2026-09-22 18:02:50，保留已批准昨天例外。没有采集新文章，没有重新启动定时。
- 37篇全部文章保持原ID集合；27篇正文完成分类、10篇显示待正文。精选12篇，新增仅Jev；原15篇不相关内容仍未入选，未进入公司或融资抽取。
- 16次DeepSeek-V4-Pro正文加工、1次新增精选公司抽取；0次新Manus请求。其余复用成功缓存。最终审核重建全部使用缓存，不能将其modelCalls=0误报为本轮没有真实模型请求。
- 公司225主体、257关联产品；42待归属主体保留内部证据和既有展示门槛。融资3家公司、2篇文章，未新增请求。

## 修复及证据边界

非精选正文采用独立library-v1提示词与空AI维度；精选原v4成功缓存沿用。前端区分待正文、待打标、打标失败，合法分类不被摘要失败掩盖。模型系统性故障保持原有发布保护，单条内容问题隔离。

Jev本次响应的发布证据与正文仅空格和换行不同。新增只规范化Unicode空白的连续匹配，仍拒绝改词、删标点或跨内容拼接；重放本次已保存响应成功，没有再发请求。此前失败轮未保存完整JSON，不能把本次证据反推为旧响应。

销量突破记录不再当新品发布；真新品发布同时包含销量时不降级。4条摘要审校修复主线截断、臆定年份及个人观点泛化，绑定文章ID、URL、正文SHA256与原摘要SHA256，不改原缓存。公司抽取审校去除社区、履历和泛比较误认；开发/运营、自有、使用及待归属关系分别保留。修正OpenAI误挂Opus的旧关系，原始提及证据不删除。

新增完整响应与输入只保留在私有work；公开投影和新增模型缓存不包含原文正文或完整响应。历史缓存条目保持不变。

## 验证与发布

769项Python离线测试、43项Node测试、TypeScript检查和静态构建通过。Windows构建进程退出清理时出现既有libuv断言，构建脚本已验证静态输出完整并成功退出；GitHub Linux构建仍以实际结果为准。

候选`3acfad13445246b78ccbe38c60d7b897`通过集合、筛选隔离、摘要绑定、产品关系、隐私投影与跨产物校验，已本地晋升。提交后的Pages部署与live JSON哈希需单独验收。

私有记录：`work/news-daily-rollout-20260922/classification-debug/`，包括真实请求、验证重放、最终缓存重建和`candidate-acceptance.json`。所有源覆盖限制沿9月22日真实扫描报告，不因打标补齐宣称20源全部通过。
