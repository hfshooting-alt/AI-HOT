# 2026-09-18 定时运行验收与公司 ID 修复

运行：[35314232820](https://github.com/hfshooting-alt/AI-HOT/actions/runs/35314232820)，任务 105502355847，基于 55a770f。以下时刻均为北京时间。

## 实际结果

- 14:17:18 创建，15:55:38 结束，结果 failure；距目标 09:30 延迟 4 小时 47 分 18 秒。固定业务窗口仍为 9 月 17 日 09:30 至 18 日 09:30，不随实际触发移动。
- 20 个 Manus 新闻任务均启动。14:30:47 三组发现契约通过：group_a 2 篇、group_b 0 篇、group_c 5 篇；流水线记录 18 个来源未完整覆盖。此前 API 只读回收显示硅星人在停止后交付 complete 结果，但流水线按止损部分结果记录，两个统计口径不可混用。
- 14:30:52 云端正文阶段成功 7/7，失败 0。这证明七篇正文实际交给了新闻阶段；机器之心本轮没有返回文章，其新增正文交接字段仍未得到真实交付验证。
- 15:21:44 新闻阶段完成，日志“正文加工：新增 408 条（缓存命中 0 条）”；这是加工数量，不是最终发布条数，也不等于 408 篇都是当天新文章。
- 15:55:35 overview 阶段报“公司 id 缺失或重复”。融资、正式数据提交和新批次 Pages 发布未完成。网站继续保留此前数据与独立人工审核补录，不能称今天自动更新成功。
- 本轮新闻 Manus 任务 API 费用合计 416 credits。公司搜索在 build 成功后执行，本次在 build 校验已失败，未进入公司搜索；模型加工费用不能从不含用量的日志推算。

## 可复现代码缺陷与修复

公司 ID 使用归一化名称生成，归属审核寻找既有公司却使用原始名称完全匹配。当前规则的 GLM-5.3 → Z .AI 与既有 Z.ai 触发了确定的离线复现：同一 ID company:f7a386a451b79627 出现两条记录。

修复为按同一公司名称归一化口径寻找审核目标，复用既有 ID、字段及新闻，保留原名称为别名；同一记录的拼写修正不执行自我合并。不使用产品别名猜测主体。重复 ID 校验仍保留，并打印具体 ID 和冲突名称，便于下一次从日志直接定位。

云端旧日志未打印具体冲突主体，且未保留公司抽取缓存，因此不能断言线上唯一冲突就是 Z.ai。上述为已复现并修复的同类真实缺陷，下一轮仍需验收。

验证：382 项 Python 离线测试通过；额外以现有全库及 GLM-5.3 回归样本重放审核，通过公司库完整校验，重复 ID 为 0。未调用付费接口，未修改正式数据。

## 恢复限制与后续

当前 CI Artifact 仅包含 state.json 和脱敏诊断（2009 字节），没有候选快照、新闻模型缓存或公司抽取缓存。日志成功不代表这些结果已持久保存；不能通过修改 state/fingerprint 宣称可续跑。私有 work/audit-20260918 已保存七篇 Manus 元数据、正文及本次日志，可复用采集结果；但它们没有包含云端后续模型输出。

本次不启动新的付费任务、不重新运行完整采集。恢复今日整批需要先确定结果保存与恢复机制，再明确所需模型重加工范围；不能将一次本地补录冒充整批自动发布成功。09:30 平台触发延迟、Manus 低产出和完整窗口覆盖仍是独立未决项。
