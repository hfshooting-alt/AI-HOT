# 2026-09-22 已保存 Manus 正文补处理

用户确认继续处理前次正文解析失败后保存的文章，并与 AIHOT 新闻合并发布。GitHub 定时和 Codex 跟进仍关闭。本次未重新扫描20源，未创建 Manus 任务，也未重试原批次4个公司超时。

## 输入与新闻去向

基线为 `3e946fc04b1c3df251ea37e886e7efa8d243383c`：400篇 AIHOT 新闻、684个正式主体、1525个关联产品。复用原手动运行 `35678966956` 的发现记录及7篇已保存正文，窗口保持北京时间 `2026-09-21 09:30` 至 `2026-09-22 09:30`。所有文章均以原始发布时间入窗，不使用点赞、评论或网页访问时间。

| 来源 | 标题 | 结果 |
| --- | --- | --- |
| ZFinance | 深度｜豌豆荚往事：中国互联网最体面的「失败者」，如何成为AI时代的黄埔军校 | 收录；历史回顾，泛行业新闻 |
| 投资界 | 投资人建议具身创始人：别纠结估值，赶紧拿钱 | 收录；行业评论，未作为新融资事件 |
| 赛博禅心 | 简单讲讲 Agent 的工作原理 | 收录；技术科普，未制造公司或具名产品 |
| 硅星人 | 简单讲讲 Agent 的工作原理 | 与上条合并，保留两个来源链接 |
| 白鲸出海 | 年销数亿的超便携电子纸操盘人，想做超便携的Everything｜对话创始人 | 排除；主线为电子纸创业，AI仅未来后台工具设想 |
| 白鲸出海 | 猫咪变悍匪，“离谱”游戏月入千万 | 排除；普通游戏分析，仅封面由AI生成 |
| 白鲸出海 | 被中国挤出的韩国游戏厂商，重新在Steam发了财 | 排除；普通游戏市场分析，仅顺带提及AI用工 |

7篇正文形成6条独立候选：3条收录、3条相关性排除，无新增新闻隔离。合并后的新闻集合为403条（400 AIHOT、3 Manus），旧400条内容保持。正文可用数与去重候选数、发布数分开记录；不能据3条发布宣称20信源完整24小时覆盖。

## 后处理及修复

共13次 DeepSeek-V4-Pro 请求，服务端记录86,479 tokens，包含6次筛选、3次摘要、3次公司抽取和1次明确的输出截断恢复。没有额外网页研究、融资抽取或 Manus credits。既有公司研究预算和研究记录保持。

ZFinance长文的第一次公司响应恰达6144输出token，并停在未闭合JSON字符串内。仅对此已确认的截断显式恢复一次，其他两篇成功缓存复用；最终3篇公司处理均完成，初次失败记录仍保留。旧批次4个公司超时继续隔离，不能算成本次补处理成功。

新闻摘要复核修正了长文只写早期收购史、漏掉后半篇AI人才主线的问题。生产摘要提示词升到v4，现有v3模型结果保持原身份，人工摘要修正另存记录。今后供应商明确返回 `finish_reason=length` 时记为 `output_limit`，保留已用token且只隔离该篇，不自动提高上限、重试或把它作为服务故障累计熔断。

公司复核根据已保存原文处理：不从中文报道推断国家，不把创业年份当法人注册日期，不把同一创业者的多个产品自动归为同一公司，不把单纯履历名单与投资人评论当成公司的新业务事件。历史估值保留年份，评论中的行业融资总量、市值与匿名金额不写进某家公司融资字段。原模型缓存与响应均保留，审校只修改派生候选。

复核后正式主体702（新增18）、关联产品1549（新增24）、待归属217（新增8）。公司输入合计403篇，399完成、原批次4篇超时保留。新增3篇均完成，其中Agent科普返回空实体是合法结果。ZFinance的62条原提议审为20公司及8个待归属具名产品；投资界13条审为9条。884个无关公司/待归属/排除记录完整保留，融资文件字节不变。

三篇的完整抽取审校另存入持久规则，精确绑定文章ID、标题、URL与正文SHA256；成功缓存复用时仍会应用，避免以后重新引入已纠正的公司/产品错误。规则不改写原缓存、不把抽取失败冒充成功，也不会作用于融资抽取。

## 发布门禁

通过基线指纹锁定、逐篇收录去向、公司证据与旧记录保持、融资文件不变、候选契约及标准 `publish-candidate` 后发布。原GitHub运行的正文失败和0条Manus发布事实保持；本次属于独立审校补录。本地已通过两次标准候选晋升：补录候选与保留历史报告/缓存格式的整理候选。最终线上提交与Pages回执保存在私有验收记录；本次公开运行摘要另存 `manus-supplement-20260922.json`，不覆盖既有历史报告。

570项完整离线回归通过。摘要提示词改进尚未用新付费样本验证，不将离线通过称为模型质量保证。私有正文、请求响应、完整审校及候选哈希位于 `work/manus-supplement-20260922/`，不提交到公开仓库。
