# 2026-09-22 手动完整运行验收

用户先取消定时，再授权手动执行一次完整工作流。本次不证明定时准点，也不恢复任何定时器。

## 运行与发布证据

- GitHub schedule 已从 `fetch-manus.yml` 移除，Codex `ai-hot` 跟进已暂停；其余工作流无 schedule。
- [手动运行 35678966956](https://github.com/hfshooting-alt/AI-HOT/actions/runs/35678966956)：代码 `582638585d25b78ab0fc237383fdb1c25c45107a`，北京时间 10:18:12 创建，11:25:42 结束，Actions 结论 success。
- 参数：`date=2026-09-22, stage=all, source_mode=full, promote=true, dry_run=false, skip_search=false`。只提交这一次采集，不重复 dispatch。
- 固定窗口：`[2026-09-21T09:30:00+08:00, 2026-09-22T09:30:00+08:00)`，实际启动延迟未移动窗口。
- 原批次数据提交 `45d9c93cd09299b07f75fc00d36fa281e89a3eb1`；[Pages 35683107836](https://github.com/hfshooting-alt/AI-HOT/actions/runs/35683107836) 成功。11:27 北京时间对线上 snapshot、company-overview、funding-table、company-profile-review 四份 JSON 逐字节校验，均与该提交一致；最后一份仍是历史定向复核报告，不作为今日研究证明。
- 加密恢复包 39,017,453 bytes、状态包 3,539 bytes 已下载；恢复包在独立私有目录成功解密，原 state/fingerprint 未改写。

## 真实结果与故障

Actions 成功只代表成功来源完成发布。本批为降级发布，不能认定双路全通过。

| 阶段 | 结果 |
| --- | --- |
| AIHOT | 获得459条，去重后450候选；45条相关性排除，5条摘要/分类证据隔离，发布400条 |
| Manus发现 | 20源全部创建，按最多3个并发依次执行；回收7篇候选，全部为窗口内原始绝对发布时间，无“昨天”例外 |
| Manus正文 | 原进程报 `double free or corruption (!prev)`，4秒后以 `-6` 退出；没有正文批次落盘，最终发布0条 |
| 公司抽取 | 400篇输入，396成功、4个90秒请求超时；4篇新闻保留，已有公司档案保留，无待执行文章 |
| 融资 | 5篇抽取成功，形成6条公司记录 |
| 定向网页研究 | 5主体、预留8页、5次模型请求；实际7次网页请求，1页共享缓存；新增Broadcom与ZAKA各1个业务字段 |

7篇来自白鲸出海3、ZFinance1、投资界1、赛博禅心1、硅星人1。发现证据已随恢复包保留，不能把正文进程故障解释为这些来源“没有新闻”。其余15源未交付合格候选，也不能据此证明无更新或无抓取能力。20任务已确认终态；观察区间峰值3不等于精确远端执行重叠证明。

正文进程原生内存错误发生在共享进程并发提取期间，Python的`except`不能隔离原生崩溃。没有原生调用栈，不能指定某个trafilatura/lxml函数为根因。后续修复以每篇提取进程隔离故障，避免一篇导致整批进程死亡。

5篇新闻隔离均有逐ID的`error.category=content`记录；原始新闻模型响应未保存，不能进一步确定是新品证据缺失、长度或原文匹配失败。4篇公司超时之间有成功调用，不触发连续失败熔断；失败缓存、公开`articleFailures`和模型错误日志一致。

## 增量与费用

- 原批次公司：620→686，新增69、离开正式公司表3；产品1363→1527，待归属177→208。资料不全不等于抽取未执行。
- 新闻 Manus 425 credits，观察阈值合计400并非账单硬上限；独立公司链接发现6 credits。没有新增测试任务或扩大预算。
- 1261个成功模型响应，记录1,595,223 tokens，另有4个超时；超时可能也产生供应商费用，未查询账单，不换算人民币。
- 阶段耗时：AIHOT约61秒，与Manus发现11分37秒并行；新闻筛选/摘要29分55秒，公司抽取/研究24分54秒，融资12秒。本轮变慢主要在后续模型加工，不能归因于Manus一直运行。
- 当日研究租约owner为`35678966956:1`，5主体/10页/5请求预算未扩大，结果与持久账本及发布产物一致。大量背景资料缺项不在这5主体之内，本批不代表全库联网补齐。

## 修复与验收边界

对照原文准备了公司纠错候选：Qwen部门归入阿里巴巴并保留部门档案，未证实的个人主体转待归属，购买/体验关系不充当产品所有权，清除任职经历误填主营业务及评价性文字误填投资人。候选基于本轮新数据，保留最新资料、新闻、融资和原模型缓存；仅6主体涉及变化。发布须经过基线锁及标准候选审核，不能覆盖新批次。

修复已完成：正文解析改为每篇独立子进程，20秒默认上限，异常退出/超时只隔离该篇；原生日志不回传，协议输出有大小限制，子进程不继承API密钥，Linux禁用core dump。没有调高预算或重发任务。Actions摘要另列失败阶段和两路发布数，快照计数绑定原发布文件指纹，避免后来的同窗口数据被算给旧运行。

验证：561项完整离线回归通过；真实本地`os.abort`、超时、正常文章并行演练证明主进程存活、正常文章成功；原生stdout/stderr各64MiB噪声被丢弃，超限协议输出被拒绝。修复后对本轮7个已发现HTTPS链接各读取1次，不重试、不跳转、不调用Manus/模型，7/7正文通过现有批次契约。HTML及正文保存在私有修复探针目录。这是本机正文验证，不等于原GitHub运行自动变成功，也不证明所有网站/完整24小时覆盖。

公司纠错已通过新的原基线锁校验及标准审核候选晋升：400新闻保持，正式公司684、产品1525、待归属209；少的2个产品在待归属记录完整保留，最新资料及其他主体原样保留。

原批次的0条Manus发布、4个公司超时以及5篇新闻隔离记录未改写。7篇正文尚未补做模型加工和发布；后续可只处理这批已保存证据，避免再跑20个发现任务。私有任务响应、正文、缓存和详细哈希保存在`work/manual-run-20260922/`，不提交到公开仓库。
