# 待接入信源（2026-09-23）

用户要求核实腾讯搜索中跳转微信的 APPSO 是否可用；已取消 FunAI Everyday 的接入。APPSO 保留在 `config/accounts.json` 候选池，**尚未加入生产采集，生产仍为20个来源**。此候选文件不驱动采集，不代表入口已通过。

| 来源 | 本轮核实 | 尚缺证据 |
| --- | --- | --- |
| APPSO | [腾讯站内搜索](https://news.qq.com/search?query=APPSO&page=1)显示近期结果，标记为“微信公众号 APPSO”，链接类型为LNK。抽查点击跳转微信，浏览器安全策略阻止访问微信目标页；未绕过限制。 | 微信正文、发布者与原始发布时间未核验；也未核实腾讯托管账号入口。搜索结果可作为发现线索，不能证明自动采集已通。 |

代码核验：`scripts/manus_source/source_urls.py` 的腾讯文章ID匹配不接受 `LNK2026092307531500` 这类跳转ID；`scripts/direct_source/transport.py` 的允许域名不含微信，且只允许同腾讯文章ID的已核实跳转。因此现有腾讯适配器不能直接接通这条路径。浏览器策略拦截不等于微信服务器返回拒绝，也不能据此推定任何部署环境均不可采集；本轮未绕过拦截测试正文。

不能把搜索摘要作为全文、把腾讯跳转链接当腾讯托管正文，也不能虚构作者ID迁入生产白名单。FunAI Everyday 已从本轮候选新增项撤回。本轮未新增Manus或模型调用，未修改正式新闻/公司数据。

后续优化建议：

1. 首先验收新版在GitHub云端的真实采集、模型处理及发布；本地成功和Pages部署成功各有独立证据。
2. 腾讯账号身份当前分别存在 `config/manus_sources.json` 与 `scripts/manus_source/tencent_seeds.py::_AUTHORS`，应集中维护已核实ID/显示名，保留严格身份校验及新增来源的列表/正文检查。
3. 在现有模型缓存之外完善正文跨批次复用与单篇失败恢复，保留原始发布时间、内容版本和抓取证据；不能把旧批内容无条件搬入新窗口。
4. 运行报告明确区分来源不可达、列表无窗内样本、正文失败与模型失败；按最新文章发布时间与分页边界判断新鲜度，不能仅看HTTP成功或总篇数。
